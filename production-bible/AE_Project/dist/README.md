# dist/

`FINAL_PROJECT.jsx` — the single production deliverable — is generated here by `tools/build.js` at the
packaging stage (Stage 21). It is the only file an artist needs to run in After Effects 2026.

Until packaging, this directory is intentionally empty (no premature dist is committed, per the staged
build contract). Build locally with `node tools/build.js`.
